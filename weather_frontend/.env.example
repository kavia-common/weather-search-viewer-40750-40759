# Frontend environment configuration (Create React App)
# Copy this file to ".env" and adjust values as needed.

# Optional: If provided, the app will fetch weather from this backend instead of Open-Meteo.
# Example: http://localhost:4000
REACT_APP_API_BASE=

# Feature flags: JSON or CSV.
# JSON example:
# {"USE_MOCK_WEATHER": false}
# CSV example:
# USE_MOCK_WEATHER=true,EXPERIMENTAL_UI=false
REACT_APP_FEATURE_FLAGS={"USE_MOCK_WEATHER": false}

# Log level for diagnostics: trace | debug | info | warn | error
REACT_APP_LOG_LEVEL=info
